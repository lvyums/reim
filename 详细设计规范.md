# 报销管理系统 - 详细设计规范

## 1. 文档信息

| 项目名称 | 报销管理系统（Reimbursement System） |
|---------|-------------------------------------|
| 文档版本 | V1.0 |
| 编写日期 | 2026-05-25 |

---

## 2. 系统概述

### 2.1 项目背景

本系统是一个企业级报销管理系统，用于管理员工的差旅报销、行程记录和补助计算。系统提供完整的报销流程管理，包括报销单创建、行程记录、补助自动计算等功能。

### 2.2 系统目标

- 实现报销单的在线填写和提交
- 自动计算出差补助金额
- 管理员工行程信息
- 提供补助标准配置和日历管理
- 支持基础数据维护（城市、业务类型等）

### 2.3 技术架构

#### 2.3.1 前端技术栈
- **框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **UI组件库**: Element Plus
- **路由管理**: Vue Router
- **状态管理**: Pinia
- **HTTP客户端**: Axios
- **开发语言**: JavaScript (ES6+)

#### 2.3.2 后端技术栈
- **框架**: Spring Boot 3.x
- **持久层**: MyBatis Plus
- **数据库**: MySQL
- **JDK版本**: JDK 24
- **构建工具**: Maven
- **包名**: com.viessmart.reimburse

#### 2.3.3 系统架构图

```
┌─────────────────────────────────────────┐
│              前端层 (Vue 3)              │
│  ┌──────────┐  ┌─────────────────────┐  │
│  │ MainView │  │    FormView         │  │
│  │ (列表页) │  │  (表单详情页)        │  │
│  └────┬─────┘  └──────────┬──────────┘  │
│       │                   │             │
│       └────────┬──────────┘             │
│                │ HTTP/REST API          │
────────────────┼────────────────────────┘
                 │
┌────────────────▼────────────────────────
│           后端层 (Spring Boot)           │
│  ┌─────────────────────────────────   │
│  │        Controller 层            │   │
│  │  - ReimFormController           │   │
│  │  - ReimItineraryController      │   │
│  │  - ReimSubsidyController        │   │
│  │  - SubsidyCalendarController    │   │
│  │  - BaseController               │   │
│  └──────────────┬──────────────────┘   │
│                 │                       │
│  ┌──────────────▼──────────────────┐   │
│  │        Service 层               │   │
│  │  - ReimFormService              │   │
│  │  - ReimItineraryService         │   │
│  │  - ReimSubsidyService           │   │
│  │  - SubsidyCalendarService       │   │
│  │  - BaseDataService              │   │
│  └──────────────┬──────────────────┘   │
│                 │                       │
│  ┌──────────────▼──────────────────┐   │
│  │        Mapper 层                │   │
│  │  - ReimFormMapper               │   │
│  │  - ReimItineraryMapper          │   │
│  │  - ReimSubsidyMapper            │   │
│  │  - SubsidyCalendarMapper        │   │
│  └──────────────┬──────────────────┘   │
└────────────────┼────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│           数据层 (MySQL)                 │
│  - reim_form (报销单表)                  │
│  - reim_itinerary (行程表)               │
│  - reim_subsidy (补助表)                 │
│  - reim_subsidy_calendar (补助日历表)    │
│  - base_city (城市表)                    │
│  - base_business_type (业务类型表)       │
│  - reim_company (公司表)                 │
│  - reim_department (部门表)              │
│  - reim_employee (员工表)                │
│  - reim_status_log (状态日志表)          │
└─────────────────────────────────────────┘
```

---

## 3. 功能需求详细描述

### 3.1 功能模块划分

系统主要包含以下功能模块：

1. **报销单管理模块**
2. **行程管理模块**
3. **补助管理模块**
4. **补助日历模块**
5. **基础数据管理模块**

### 3.2 功能点详细说明

#### 3.2.1 报销单管理模块

##### 3.2.1.1 功能描述
报销单管理是系统的核心功能，负责管理员工的报销申请。每个报销单可以包含多个行程记录和对应的补助信息。

##### 3.2.1.2 业务流程
1. 用户创建新的报销单
2. 填写报销单基本信息（申请人、部门、公司等）
3. 添加行程记录（出发地、目的地、时间等）
4. 系统自动计算补助金额
5. 提交报销单进行审核

##### 3.2.1.3 数据模型
**表名**: `reim_form`

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PK, AUTO_INCREMENT |
| form_number | VARCHAR(50) | 报销单号 | UNIQUE, NOT NULL |
| applicant_id | BIGINT | 申请人ID | NOT NULL |
| applicant_name | VARCHAR(50) | 申请人姓名 | NOT NULL |
| department_id | BIGINT | 部门ID | NOT NULL |
| department_name | VARCHAR(100) | 部门名称 | NOT NULL |
| company_id | BIGINT | 公司ID | NOT NULL |
| company_name | VARCHAR(100) | 公司名称 | NOT NULL |
| business_type_id | BIGINT | 业务类型ID | NOT NULL |
| business_type_name | VARCHAR(50) | 业务类型名称 | NOT NULL |
| total_amount | DECIMAL(10,2) | 总金额 | DEFAULT 0.00 |
| status | TINYINT | 状态 | NOT NULL |
| create_time | DATETIME | 创建时间 | NOT NULL |
| update_time | DATETIME | 更新时间 | NOT NULL |

##### 3.2.1.4 接口定义

**创建报销单**
- **URL**: `POST /api/reim/form/create`
- **参数**: 
  ```json
  {
    "applicantId": 1,
    "applicantName": "张三",
    "departmentId": 10,
    "departmentName": "技术部",
    "companyId": 1,
    "companyName": "XX公司",
    "businessTypeId": 1,
    "businessTypeName": "差旅"
  }
  ```
- **返回**:
  ```json
  {
    "code": 200,
    "message": "success",
    "data": {
      "id": 1,
      "formNumber": "RF20260525001"
    }
  }
  ```

**查询报销单列表**
- **URL**: `GET /api/reim/form/list`
- **参数**: 
  - page: 页码
  - size: 每页大小
  - keyword: 搜索关键字（可选）
- **返回**: 分页的报销单列表

**查询报销单详情**
- **URL**: `GET /api/reim/form/{id}`
- **参数**: id - 报销单ID
- **返回**: 报销单详细信息，包含行程和补助数据

**删除报销单**
- **URL**: `DELETE /api/reim/form/{id}`
- **参数**: id - 报销单ID
- **返回**: 操作结果

#### 3.2.2 行程管理模块

##### 3.2.2.1 功能描述
行程管理模块用于记录员工的出差行程信息，包括出发地、目的地、出发时间、到达时间等。每个报销单可以包含多个行程记录。

##### 3.2.2.2 数据模型
**表名**: `reim_itinerary`

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PK, AUTO_INCREMENT |
| form_id | BIGINT | 报销单ID | FK, NOT NULL |
| departure_city_id | BIGINT | 出发城市ID | NOT NULL |
| departure_city_name | VARCHAR(50) | 出发城市名称 | NOT NULL |
| arrival_city_id | BIGINT | 到达城市ID | NOT NULL |
| arrival_city_name | VARCHAR(50) | 到达城市名称 | NOT NULL |
| departure_time | DATETIME | 出发时间 | NOT NULL |
| arrival_time | DATETIME | 到达时间 | NOT NULL |
| transport_mode | VARCHAR(20) | 交通方式 | NOT NULL |
| sort_order | INT | 排序序号 | NOT NULL |
| create_time | DATETIME | 创建时间 | NOT NULL |
| update_time | DATETIME | 更新时间 | NOT NULL |

##### 3.2.2.3 接口定义

**创建行程**
- **URL**: `POST /api/reim/itinerary/create`
- **参数**:
  ```json
  {
    "formId": 1,
    "departureCityId": 1,
    "departureCityName": "北京",
    "arrivalCityId": 2,
    "arrivalCityName": "上海",
    "departureTime": "2026-05-25 08:00:00",
    "arrivalTime": "2026-05-25 12:00:00",
    "transportMode": "高铁",
    "sortOrder": 1
  }
  ```

**更新行程**
- **URL**: `PUT /api/reim/itinerary/update`
- **参数**: 行程完整信息

**删除行程**
- **URL**: `DELETE /api/reim/itinerary/{id}`

**批量保存行程**
- **URL**: `POST /api/reim/itinerary/batch-save`
- **参数**: 行程数组
- **说明**: 用于一次性保存多个行程记录

#### 3.2.3 补助管理模块

##### 3.2.3.1 功能描述
补助管理模块根据行程信息自动计算出差补助金额。补助金额根据出发城市、到达城市、补助标准等因素计算。

##### 3.2.3.2 数据模型
**表名**: `reim_subsidy`

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PK, AUTO_INCREMENT |
| form_id | BIGINT | 报销单ID | FK, NOT NULL |
| itinerary_id | BIGINT | 行程ID | FK, NOT NULL |
| subsidy_date | DATE | 补助日期 | NOT NULL |
| city_id | BIGINT | 城市ID | NOT NULL |
| city_name | VARCHAR(50) | 城市名称 | NOT NULL |
| subsidy_standard | DECIMAL(10,2) | 补助标准 | NOT NULL |
| subsidy_amount | DECIMAL(10,2) | 补助金额 | NOT NULL |
| subsidy_days | DECIMAL(5,2) | 补助天数 | NOT NULL |
| create_time | DATETIME | 创建时间 | NOT NULL |
| update_time | DATETIME | 更新时间 | NOT NULL |

##### 3.2.3.3 补助计算规则
1. 根据行程的出发时间和到达时间计算补助天数
2. 根据城市和补助标准确定每日补助金额
3. 补助金额 = 补助天数 × 补助标准
4. 支持跨天行程的补助计算

##### 3.2.3.4 接口定义

**计算补助**
- **URL**: `POST /api/reim/subsidy/calculate`
- **参数**: formId - 报销单ID
- **返回**: 计算后的补助列表
- **说明**: 根据行程信息自动计算所有补助

**保存补助**
- **URL**: `POST /api/reim/subsidy/save`
- **参数**: 补助数组
- **说明**: 保存计算后的补助数据

**查询补助列表**
- **URL**: `GET /api/reim/subsidy/list/{formId}`
- **参数**: formId - 报销单ID
- **返回**: 该报销单的所有补助记录

#### 3.2.4 补助日历模块

##### 3.2.4.1 功能描述
补助日历模块用于管理不同日期的补助标准。可以设置特定日期的补助系数或特殊补助规则。

##### 3.2.4.2 数据模型
**表名**: `reim_subsidy_calendar`

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PK, AUTO_INCREMENT |
| calendar_date | DATE | 日历日期 | UNIQUE, NOT NULL |
| subsidy_coefficient | DECIMAL(5,2) | 补助系数 | DEFAULT 1.00 |
| is_holiday | TINYINT | 是否节假日 | DEFAULT 0 |
| holiday_type | VARCHAR(20) | 节假日类型 | NULL |
| remark | VARCHAR(200) | 备注 | NULL |
| create_time | DATETIME | 创建时间 | NOT NULL |
| update_time | DATETIME | 更新时间 | NOT NULL |

##### 3.2.4.3 接口定义

**查询补助日历**
- **URL**: `GET /api/subsidy/calendar/query`
- **参数**: 
  - startDate: 开始日期
  - endDate: 结束日期
- **返回**: 指定日期范围内的补助日历

**保存补助日历**
- **URL**: `POST /api/subsidy/calendar/save`
- **参数**: 补助日历对象

#### 3.2.5 基础数据管理模块

##### 3.2.5.1 功能描述
基础数据管理模块提供系统所需的静态数据维护，包括城市列表、业务类型、公司信息、部门信息、员工信息等。

##### 3.2.5.2 数据模型

**城市表**: `base_city`
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| city_code | VARCHAR(20) | 城市代码 |
| city_name | VARCHAR(50) | 城市名称 |
| province | VARCHAR(50) | 省份 |
| subsidy_standard | DECIMAL(10,2) | 补助标准 |

**业务类型表**: `base_business_type`
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| type_code | VARCHAR(20) | 类型代码 |
| type_name | VARCHAR(50) | 类型名称 |
| description | VARCHAR(200) | 描述 |

**公司表**: `reim_company`
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| company_code | VARCHAR(20) | 公司代码 |
| company_name | VARCHAR(100) | 公司名称 |

**部门表**: `reim_department`
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| department_code | VARCHAR(20) | 部门代码 |
| department_name | VARCHAR(100) | 部门名称 |
| company_id | BIGINT | 所属公司ID |

**员工表**: `reim_employee`
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| employee_code | VARCHAR(20) | 员工工号 |
| employee_name | VARCHAR(50) | 员工姓名 |
| department_id | BIGINT | 所属部门ID |

##### 3.2.5.3 接口定义

**查询城市列表**
- **URL**: `GET /api/base/city/list`
- **返回**: 所有城市列表

**查询业务类型列表**
- **URL**: `GET /api/base/business-type/list`
- **返回**: 所有业务类型列表

**查询公司列表**
- **URL**: `GET /api/base/company/list`
- **返回**: 所有公司列表

**查询部门列表**
- **URL**: `GET /api/base/department/list`
- **参数**: companyId（可选）
- **返回**: 部门列表

**查询员工列表**
- **URL**: `GET /api/base/employee/list`
- **参数**: departmentId（可选）
- **返回**: 员工列表

---

## 4. 页面设计

### 4.1 页面结构

系统包含两个主要页面：

1. **MainView（主页）** - 报销单列表页
2. **FormView（表单页）** - 报销单详情编辑页

### 4.2 MainView 页面设计

#### 4.2.1 页面布局
- **顶部**: 页面标题和新建按钮
- **中部**: 搜索栏（可选）
- **主体**: 报销单列表表格
- **底部**: 分页控件

#### 4.2.2 功能组件
- **新建按钮**: 点击跳转到FormView创建新报销单
- **列表表格**: 显示报销单列表，包含以下列：
  - 报销单号
  - 申请人
  - 部门
  - 业务类型
  - 总金额
  - 状态
  - 创建时间
  - 操作列（查看、编辑、删除）
- **分页**: 支持翻页浏览

#### 4.2.3 交互逻辑
1. 页面加载时自动加载报销单列表
2. 点击"新建"按钮跳转到FormView（mode=new）
3. 点击"查看"按钮跳转到FormView（mode=view&id=xxx）
4. 点击"编辑"按钮跳转到FormView（mode=edit&id=xxx）
5. 点击"删除"按钮弹出确认框，确认后调用删除接口

### 4.3 FormView 页面设计

#### 4.3.1 页面布局
- **顶部**: 页面标题和操作按钮（保存、返回）
- **主体**: 分为三个标签页
  - Tab 1: 报销单信息
  - Tab 2: 行程管理
  - Tab 3: 补助明细

#### 4.3.2 Tab 1 - 报销单信息
**表单字段**:
- 申请人（下拉选择）
- 部门（下拉选择，根据申请人联动）
- 公司（下拉选择，根据部门联动）
- 业务类型（下拉选择）
- 报销单号（自动生成，只读）
- 总金额（自动计算，只读）
- 状态（显示当前状态）

#### 4.3.3 Tab 2 - 行程管理
**功能组件**:
- **新增行程按钮**: 添加新的行程记录
- **行程列表**: 显示所有行程，支持拖拽排序
- **行程表单**: 
  - 出发城市（下拉选择）
  - 到达城市（下拉选择）
  - 出发时间（日期时间选择器）
  - 到达时间（日期时间选择器）
  - 交通方式（下拉选择：飞机、高铁、火车、汽车等）
- **删除按钮**: 删除选中的行程

**交互逻辑**:
1. 点击"新增行程"在列表末尾添加空行程
2. 填写行程信息后自动保存
3. 支持拖拽调整行程顺序
4. 行程变化后触发补助重新计算

#### 4.3.4 Tab 3 - 补助明细
**功能组件**:
- **计算补助按钮**: 根据行程重新计算补助
- **补助列表**: 显示所有补助记录
  - 补助日期
  - 城市
  - 补助标准
  - 补助天数
  - 补助金额
- **合计行**: 显示补助总金额

**交互逻辑**:
1. 点击"计算补助"调用后端计算接口
2. 根据行程和城市补助标准自动计算
3. 考虑补助日历中的特殊日期系数
4. 计算完成后自动保存到数据库

#### 4.3.5 底部操作栏
- **保存按钮**: 保存当前报销单及所有关联数据
- **返回按钮**: 返回到MainView列表页

### 4.4 路由设计

```javascript
const routes = [
  {
    path: '/',
    redirect: '/main'
  },
  {
    path: '/main',
    name: 'Main',
    component: MainView
  },
  {
    path: '/form/:mode/:id?',
    name: 'Form',
    component: FormView,
    props: true
  }
]
```

**路由参数说明**:
- mode: new（新建）、edit（编辑）、view（查看）
- id: 报销单ID（新建时不需要）

---

## 5. 数据库设计

### 5.1 数据库ER图

```
┌──────────────────┐       ┌──────────────────┐
│   reim_form      │       │ reim_itinerary   │
│──────────────────│       │──────────────────│
│ *id (PK)         │1────M│ *id (PK)         │
│  form_number     │       │  form_id (FK)    │
│  applicant_id    │       │  departure_city  │
│  applicant_name  │       │  arrival_city    │
│  department_id   │       │  departure_time  │
│  company_id      │       │  arrival_time    │
│  business_type_id│       │  transport_mode  │
│  total_amount    │       │  sort_order      │
│  status          │       └────────┬─────────┘
│  create_time     │                │
│  update_time     │                │ 1
└────────┬─────────┘                │
         │                          │
         │ 1                        │ M
         │                          │
         │ M                        ▼
         │                 ┌──────────────────┐
         │                 │  reim_subsidy    │
         │                 │──────────────────│
         │                 │ *id (PK)         │
         │                 │  form_id (FK)    │
         │                 │  itinerary_id(FK)│
         │                 │  subsidy_date    │
         │                 │  city_id         │
         │                 │  subsidy_amount  │
         │                 └──────────────────┘

┌──────────────────┐       ┌──────────────────┐
│  base_city       │       │reim_subsidy_cal. │
│──────────────────│       │──────────────────│
│ *id (PK)         │       │ *id (PK)         │
│  city_code       │       │  calendar_date   │
│  city_name       │       │  coefficient     │
│  province        │       │  is_holiday      │
│  subsidy_standard│       │  holiday_type    │
└──────────────────┘       └──────────────────┘

┌──────────────────┐       ┌──────────────────┐
│  reim_company    │       │reim_department   │
│──────────────────│1────M│──────────────────│
│ *id (PK)         │       │ *id (PK)         │
│  company_code    │       │  dept_code       │
│  company_name    │       │  dept_name       │
──────────────────┘       │  company_id (FK) │
                           └────────┬─────────┘
                                    │
                                    │ 1
                                    │
                                    │ M
                                    ▼
                           ┌──────────────────┐
                           │ reim_employee    │
                           │──────────────────│
                           │ *id (PK)         │
                           │  emp_code        │
                           │  emp_name        │
                           │  dept_id (FK)    │
                           └──────────────────┘
```

### 5.2 表关系说明

1. **reim_form ↔ reim_itinerary**: 一对多关系
   - 一个报销单可以有多个行程
   - 通过 form_id 关联

2. **reim_itinerary ↔ reim_subsidy**: 一对多关系
   - 一个行程可以产生多条补助记录（跨天时）
   - 通过 itinerary_id 关联

3. **reim_form ↔ reim_subsidy**: 一对多关系
   - 一个报销单可以有多条补助记录
   - 通过 form_id 关联

4. **base_city ↔ reim_itinerary**: 一对多关系
   - 城市被多个行程引用
   - 通过 departure_city_id 和 arrival_city_id 关联

5. **reim_company ↔ reim_department**: 一对多关系
   - 一个公司有多个部门
   - 通过 company_id 关联

6. **reim_department ↔ reim_employee**: 一对多关系
   - 一个部门有多个员工
   - 通过 department_id 关联

### 5.3 索引设计

| 表名 | 索引名 | 索引字段 | 索引类型 | 说明 |
|------|--------|----------|----------|------|
| reim_form | idx_form_number | form_number | UNIQUE | 报销单号唯一索引 |
| reim_form | idx_applicant | applicant_id | NORMAL | 按申请人查询 |
| reim_form | idx_create_time | create_time | NORMAL | 按创建时间排序 |
| reim_itinerary | idx_form_id | form_id | NORMAL | 按报销单查询行程 |
| reim_itinerary | idx_sort_order | form_id, sort_order | NORMAL | 行程排序 |
| reim_subsidy | idx_form_id | form_id | NORMAL | 按报销单查询补助 |
| reim_subsidy | idx_itinerary_id | itinerary_id | NORMAL | 按行程查询补助 |
| reim_subsidy_calendar | idx_calendar_date | calendar_date | UNIQUE | 日期唯一索引 |
| base_city | idx_city_code | city_code | UNIQUE | 城市代码唯一索引 |

---

## 6. 接口设计

### 6.1 接口规范

#### 6.1.1 统一响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

**响应码说明**:
- 200: 成功
- 400: 请求参数错误
- 401: 未授权
- 403: 禁止访问
- 404: 资源不存在
- 500: 服务器内部错误

#### 6.1.2 分页响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "page": 1,
    "size": 10,
    "records": []
  }
}
```

### 6.2 接口清单

#### 6.2.1 报销单接口

| 接口名称 | URL | 方法 | 说明 |
|---------|-----|------|------|
| 创建报销单 | /api/reim/form/create | POST | 创建新的报销单 |
| 查询列表 | /api/reim/form/list | GET | 分页查询报销单列表 |
| 查询详情 | /api/reim/form/{id} | GET | 查询报销单详情 |
| 更新报销单 | /api/reim/form/update | PUT | 更新报销单基本信息 |
| 删除报销单 | /api/reim/form/{id} | DELETE | 删除报销单 |

#### 6.2.2 行程接口

| 接口名称 | URL | 方法 | 说明 |
|---------|-----|------|------|
| 创建行程 | /api/reim/itinerary/create | POST | 创建单个行程 |
| 批量保存 | /api/reim/itinerary/batch-save | POST | 批量保存行程 |
| 更新行程 | /api/reim/itinerary/update | PUT | 更新行程信息 |
| 删除行程 | /api/reim/itinerary/{id} | DELETE | 删除行程 |
| 查询列表 | /api/reim/itinerary/list/{formId} | GET | 查询报销单的行程列表 |

#### 6.2.3 补助接口

| 接口名称 | URL | 方法 | 说明 |
|---------|-----|------|------|
| 计算补助 | /api/reim/subsidy/calculate | POST | 根据行程计算补助 |
| 保存补助 | /api/reim/subsidy/save | POST | 保存补助数据 |
| 查询列表 | /api/reim/subsidy/list/{formId} | GET | 查询报销单的补助列表 |

#### 6.2.4 补助日历接口

| 接口名称 | URL | 方法 | 说明 |
|---------|-----|------|------|
| 查询日历 | /api/subsidy/calendar/query | GET | 查询补助日历 |
| 保存日历 | /api/subsidy/calendar/save | POST | 保存补助日历 |

#### 6.2.5 基础数据接口

| 接口名称 | URL | 方法 | 说明 |
|---------|-----|------|------|
| 城市列表 | /api/base/city/list | GET | 获取城市列表 |
| 业务类型列表 | /api/base/business-type/list | GET | 获取业务类型列表 |
| 公司列表 | /api/base/company/list | GET | 获取公司列表 |
| 部门列表 | /api/base/department/list | GET | 获取部门列表 |
| 员工列表 | /api/base/employee/list | GET | 获取员工列表 |

---

## 7. 关键技术实现

### 7.1 前端关键技术

#### 7.1.1 Vue 3 Composition API
使用 Composition API 组织代码逻辑，提高代码复用性和可维护性。

**示例**:
```javascript
import { ref, reactive, onMounted } from 'vue'

export default {
  setup() {
    const formList = ref([])
    const loading = ref(false)
    
    const loadFormList = async () => {
      loading.value = true
      try {
        const res = await getFormList()
        formList.value = res.data.records
      } finally {
        loading.value = false
      }
    }
    
    onMounted(() => {
      loadFormList()
    })
    
    return {
      formList,
      loading,
      loadFormList
    }
  }
}
```

#### 7.1.2 Element Plus 组件库
使用 Element Plus 提供的丰富UI组件，快速构建界面。

**主要使用的组件**:
- el-table: 数据表格
- el-form: 表单
- el-dialog: 对话框
- el-tabs: 标签页
- el-date-picker: 日期选择器
- el-select: 下拉选择
- el-button: 按钮
- el-pagination: 分页

#### 7.1.3 Axios 拦截器
配置统一的请求和响应拦截器，处理全局逻辑。

```javascript
// 请求拦截器
axios.interceptors.request.use(config => {
  config.headers.Authorization = getToken()
  return config
})

// 响应拦截器
axios.interceptors.response.use(response => {
  if (response.data.code === 200) {
    return response.data
  } else {
    ElMessage.error(response.data.message)
    return Promise.reject(response.data)
  }
})
```

#### 7.1.4 路由守卫
使用路由守卫进行权限控制和页面跳转逻辑。

```javascript
router.beforeEach((to, from, next) => {
  // 检查登录状态
  if (!isLogin() && to.path !== '/login') {
    next('/login')
  } else {
    next()
  }
})
```

### 7.2 后端关键技术

#### 7.2.1 Spring Boot 自动配置
利用 Spring Boot 的自动配置特性，简化配置过程。

**主要配置**:
- application.yml: 应用配置
- MyBatis Plus 配置: 数据库相关配置
- CORS 配置: 跨域支持

#### 7.2.2 MyBatis Plus ORM
使用 MyBatis Plus 简化数据库操作，提供强大的CRUD功能。

**特点**:
- 内置通用Mapper，无需编写SQL
- 支持Lambda表达式查询
- 分页插件支持
- 代码生成器

**示例**:
```java
@Service
public class ReimFormServiceImpl extends ServiceImpl<ReimFormMapper, ReimForm> 
    implements ReimFormService {
    
    @Override
    public Page<ReimForm> getFormList(Page<ReimForm> page, String keyword) {
        LambdaQueryWrapper<ReimForm> wrapper = new LambdaQueryWrapper<>();
        if (StringUtils.hasText(keyword)) {
            wrapper.like(ReimForm::getFormNumber, keyword)
                   .or()
                   .like(ReimForm::getApplicantName, keyword);
        }
        wrapper.orderByDesc(ReimForm::getCreateTime);
        return this.page(page, wrapper);
    }
}
```

#### 7.2.3 统一异常处理
使用 @ControllerAdvice 和 @ExceptionHandler 实现全局异常处理。

```java
@ControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<Result> handleBusinessException(BusinessException e) {
        return ResponseEntity.ok(Result.error(e.getCode(), e.getMessage()));
    }
    
    @ExceptionHandler(Exception.class)
    public ResponseEntity<Result> handleException(Exception e) {
        log.error("系统异常", e);
        return ResponseEntity.ok(Result.error(500, "系统异常"));
    }
}
```

#### 7.2.4 事务管理
使用 @Transactional 注解管理事务，确保数据一致性。

```java
@Service
@Transactional
public class ReimFormServiceImpl implements ReimFormService {
    
    @Override
    public Long createForm(ReimFormDTO dto) {
        // 创建报销单
        ReimForm form = new ReimForm();
        BeanUtils.copyProperties(dto, form);
        form.setFormNumber(generateFormNumber());
        this.save(form);
        
        // 其他操作...
        
        return form.getId();
    }
}
```

#### 7.2.5 补助计算算法
实现智能补助计算逻辑，考虑多种因素。

**计算流程**:
1. 获取报销单的所有行程
2. 遍历每个行程，计算补助天数
3. 根据城市查询补助标准
4. 查询补助日历，获取特殊日期系数
5. 计算每条补助记录的金额
6. 汇总总补助金额

**核心代码**:
```java
public List<ReimSubsidy> calculateSubsidy(Long formId) {
    // 获取行程列表
    List<ReimItinerary> itineraries = itineraryService.listByFormId(formId);
    
    List<ReimSubsidy> subsidies = new ArrayList<>();
    
    for (ReimItinerary itinerary : itineraries) {
        // 计算补助天数
        BigDecimal days = calculateDays(itinerary.getDepartureTime(), 
                                       itinerary.getArrivalTime());
        
        // 获取城市补助标准
        BaseCity city = cityService.getById(itinerary.getArrivalCityId());
        BigDecimal standard = city.getSubsidyStandard();
        
        // 查询补助日历系数
        BigDecimal coefficient = getSubsidyCoefficient(itinerary.getArrivalTime());
        
        // 计算补助金额
        BigDecimal amount = days.multiply(standard).multiply(coefficient);
        
        // 创建补助记录
        ReimSubsidy subsidy = new ReimSubsidy();
        subsidy.setFormId(formId);
        subsidy.setItineraryId(itinerary.getId());
        subsidy.setSubsidyDate(itinerary.getArrivalTime().toLocalDate());
        subsidy.setCityId(city.getId());
        subsidy.setSubsidyStandard(standard);
        subsidy.setSubsidyDays(days);
        subsidy.setSubsidyAmount(amount);
        
        subsidies.add(subsidy);
    }
    
    return subsidies;
}
```

---

## 8. 安全性设计

### 8.1 数据安全

#### 8.1.1 SQL注入防护
- 使用 MyBatis Plus 的参数化查询
- 避免拼接SQL字符串
- 对用户输入进行验证和过滤

#### 8.1.2 XSS防护
- 前端对用户输入进行转义
- 后端对输出数据进行HTML编码
- 使用 Content-Security-Policy 头

#### 8.1.3 敏感数据保护
- 密码等敏感信息加密存储
- 传输层使用 HTTPS
- 日志中不记录敏感信息

### 8.2 访问控制

#### 8.2.1 身份认证
- 使用 JWT Token 进行身份认证
- Token 存储在 HttpOnly Cookie 中
- 设置合理的过期时间

#### 8.2.2 权限控制
- 基于角色的访问控制（RBAC）
- 接口级别的权限校验
- 数据级别的权限隔离

### 8.3 审计日志

#### 8.3.1 操作日志
记录关键操作日志，包括：
- 用户登录/登出
- 数据创建/修改/删除
- 重要业务操作

#### 8.3.2 日志内容
- 操作时间
- 操作用户
- 操作类型
- 操作对象
- 操作结果
- IP地址

---

## 9. 性能优化

### 9.1 前端性能优化

#### 9.1.1 代码分割
- 使用路由懒加载
- 大型组件异步加载
- 第三方库按需引入

#### 9.1.2 资源优化
- 图片压缩和懒加载
- CSS/JS 压缩和混淆
- 使用 CDN 加速静态资源

#### 9.1.3 缓存策略
- 使用浏览器缓存
- API 响应缓存
- 本地存储常用数据

### 9.2 后端性能优化

#### 9.2.1 数据库优化
- 合理使用索引
- 避免 N+1 查询问题
- 使用分页查询大数据量
- 定期清理历史数据

#### 9.2.2 缓存机制
- 使用 Redis 缓存热点数据
- 缓存基础数据（城市、业务类型等）
- 设置合理的缓存过期策略

#### 9.2.3 并发处理
- 使用线程池处理异步任务
- 合理设置数据库连接池
- 避免长时间持有锁

---

## 10. 部署方案

### 10.1 环境要求

#### 10.1.1 前端环境
- Node.js 16+
- npm 或 yarn
- 现代浏览器（Chrome、Firefox、Edge等）

#### 10.1.2 后端环境
- JDK 24
- Maven 3.6+
- MySQL 8.0+
- Tomcat 9+（或使用内置Tomcat）

### 10.2 部署步骤

#### 10.2.1 前端部署
1. 安装依赖: `npm install`
2. 构建生产版本: `npm run build`
3. 将 dist 目录部署到 Nginx 或其他Web服务器

**Nginx 配置示例**:
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /path/to/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 10.2.2 后端部署
1. 打包应用: `mvn clean package`
2. 运行JAR包: `java -jar reimburse-system.jar`
3. 或使用 Docker 容器化部署

**Dockerfile 示例**:
```dockerfile
FROM openjdk:24-jdk-slim
VOLUME /tmp
COPY target/reimburse-system.jar app.jar
ENTRYPOINT ["java","-jar","/app.jar"]
```

### 10.3 配置文件

#### 10.3.1 前端配置
**.env.production**:
```
VITE_API_BASE_URL=http://your-api-domain.com/api
```

#### 10.3.2 后端配置
**application-prod.yml**:
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reimburse_db?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: your-password
    
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
    
server:
  port: 8080
```

---

## 11. 测试方案

### 11.1 单元测试

#### 11.1.1 前端单元测试
- 使用 Vitest 或 Jest
- 测试组件渲染
- 测试事件处理
- 测试API调用

#### 11.1.2 后端单元测试
- 使用 JUnit 5
- 测试 Service 层逻辑
- 测试 Controller 层接口
- 使用 Mock 隔离依赖

### 11.2 集成测试

#### 11.2.1 API 测试
- 使用 Postman 或 Swagger
- 测试所有接口
- 验证请求和响应格式
- 测试边界条件

#### 11.2.2 E2E 测试
- 使用 Cypress 或 Playwright
- 模拟用户操作流程
- 测试页面交互
- 验证数据正确性

### 11.3 性能测试

#### 11.3.1 压力测试
- 使用 JMeter 或 LoadRunner
- 模拟多用户并发访问
- 测试系统吞吐量
- 监控响应时间

#### 11.3.2 性能指标
- 接口响应时间 < 500ms
- 并发用户数 >= 100
- 系统可用性 >= 99.9%

---

## 12. 运维监控

### 12.1 日志管理

#### 12.1.1 日志级别
- DEBUG: 开发调试
- INFO: 一般信息
- WARN: 警告信息
- ERROR: 错误信息

#### 12.1.2 日志收集
- 使用 ELK Stack（Elasticsearch、Logstash、Kibana）
- 集中管理应用日志
- 实时日志分析

### 12.2 监控告警

#### 12.2.1 应用监控
- 使用 Prometheus + Grafana
- 监控 JVM 指标
- 监控接口响应时间
- 监控错误率

#### 12.2.2 告警机制
- 设置阈值告警
- 邮件/短信通知
- 自动恢复机制

### 12.3 备份恢复

#### 12.3.1 数据备份
- 定期备份数据库
- 备份频率：每天全量备份
- 保留周期：30天

#### 12.3.2 灾难恢复
- 制定应急预案
- 定期演练恢复流程
- RTO < 4小时，RPO < 1小时

---

## 13. 附录

### 13.1 术语表

| 术语 | 说明 |
|------|------|
| 报销单 | 员工提交的报销申请单据 |
| 行程 | 出差的起止记录 |
| 补助 | 根据行程计算的出差补贴 |
| 补助日历 | 记录特殊日期补助系数的日历 |
| 业务类型 | 报销的业务分类（如差旅、办公等） |

### 13.2 参考资料

1. Vue 3 官方文档: https://vuejs.org/
2. Element Plus 文档: https://element-plus.org/
3. Spring Boot 文档: https://spring.io/projects/spring-boot
4. MyBatis Plus 文档: https://baomidou.com/

### 13.3 联系方式

如有问题，请联系：
- 项目负责人: zyj17
- 邮箱: zyj17@example.com
- 电话: xxx-xxxx-xxxx

---

**文档结束**
